import React from 'react';
import PropTypes from 'prop-types';

const shop = props => {
    return (
        <div>
            Blog
        </div>
    );
};

shop.propTypes = {
    
};

export default shop;